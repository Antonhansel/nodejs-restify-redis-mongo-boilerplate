api:
  environment:
   - NODE_ENV=development
  build: .
  ports:
    - "8000:8000"
  links:
    - "redis"
    - "mongo"
  volumes:
   - ./:/usr/src/app/
   - /usr/src/app/node_modules

redis:
  image: redis

mongo:
 image: mongo
